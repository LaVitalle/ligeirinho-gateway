{
  "name": "@ligeirinho/gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Ligeirinho Food — API Gateway (reverse proxy + Swagger agregado)",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "start:dev": "tsx watch src/main.ts",
    "start:prod": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "~5.7"
  }
}
